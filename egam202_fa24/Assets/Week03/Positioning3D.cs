using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Positioning3D : MonoBehaviour
{

    public Vector3 position;


    // Start is called before the first frame update
    void Start()
    {
        //Vector3.forward;

        //new Vector3(0, 0, 1);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
